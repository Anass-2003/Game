import random
ran = random.randint(0,11)
enter =int(input("Guess a number betwen 1 an 10    "))
while ran != enter:
    if ran < enter :
        print("too hight.try again:   ")
        enter =int(input("Guess a number betwen 1 an 10    "))
        
    else: 
        print("too low . try again:   ")
        enter =int(input("Guess a number betwen 1 an 10    "))
print("congratulations, you guess the number")
    
        
